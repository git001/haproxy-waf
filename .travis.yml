sudo: required

language: c

services:
  - docker

before_install:
  - docker build -t me2digital/haproxy-waf .
  - docker run --entrypoint /bin/bash --rm me2digital/haproxy-waf haproxy -vv

script:
  - haproxy -vv
